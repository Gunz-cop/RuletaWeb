name: CI

# El sitio no tiene backend ni suite de comportamiento: lo que se puede romper
# sin avisar es el CSS. Este workflow construye el sitio y comprueba que las
# reglas que llegan a cada página siguen siendo las mismas.

on:
  push:
    branches: ['**']
  pull_request:

# Un push nuevo sobre la misma rama cancela el build anterior: el build tarda
# más de un minuto por el procesado de imágenes y no interesa acumular colas.
# El workflow solo lee el repositorio: no publica, no comenta, no despliega.
permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  css:
    name: Build y snapshots de CSS
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      # Ejecuta build + paridad de tokens + colisiones + snapshots. Ver AGENTS.md.
      - run: npm test

      # Los tres tests anteriores miran la página en reposo. Este pulsa
      # botones y comprueba los estilos computados después: es lo único que
      # detecta que una regla ha dejado de encontrar su elemento, porque el
      # CSS se sigue emitiendo igual aunque ya no se aplique a nadie.
      - run: npx playwright install --with-deps chromium
      - run: npm run test:estado

      # Si los snapshots fallan, el diff de arriba dice qué reglas cambiaron.
      # Esto deja además el dist/ construido a mano para poder inspeccionarlo.
      - name: Guarda el CSS construido cuando algo falla
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: dist-css
          path: dist/_astro/*.css
          retention-days: 7
