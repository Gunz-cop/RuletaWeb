---
import Layout from '../layouts/Layout.astro';
import HomeHero from '../components/HomeHero.astro';
import HomeHowItWorks from '../components/HomeHowItWorks.astro';
import RouletteMachine from '../components/RouletteMachine.astro';
import HomeToolsSection from '../components/HomeToolsSection.astro';
import WinnerModal from '../components/WinnerModal.astro';
import AdSlot from '../components/AdSlot.astro';
import HubGrid from '../components/HubGrid.astro';
import SeoArticle from '../components/SeoArticle.astro';
import { getSocialImageUrl } from '../lib/heroImages';

// Experimento de SEO: la raíz nunca tuvo un <title>/<description> que
// dijera "ruleta" -- se prueba esta redacción para ver si decidelo.app
// empieza a rankear por búsquedas de ruleta antes de decidir si la
// herramienta se muda a su propia página (/ruleta). Ver AGENTS.md sobre
// Search Console para el seguimiento. No toca el <h1> ni el JSON-LD: son
// cambios aparte, deliberadamente medibles por separado.
const title = 'Ruleta Aleatoria Online Gratis — Decídelo.app';
const description = 'Ruleta aleatoria online para sortear nombres, elegir al azar y resolver empates. Gratis, sin registro, 100% en tu navegador. Además: moneda, dados y equipos.';

// La imagen vive en src/assets y la optimiza Astro; el JSON-LD necesita
// su URL absoluta ya generada, no la ruta pública que tenía antes.
const socialImage = await getSocialImageUrl('/blog/ruleta.png');

const schemaLD = {
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "WebApplication",
      "@id": "https://decidelo.app/#webapp",
      "name": "Decídelo.app",
      "description": "Hub de herramientas de decisión aleatoria: ruleta online, lanzar moneda, dados virtuales, generador de números y más. Gratuito, privado y sin registro.",
      "url": "https://decidelo.app/",
      "applicationCategory": "UtilityApplication",
      "operatingSystem": "All",
      "browserRequirements": "Requires JavaScript and HTML5 Canvas",
      "softwareVersion": "2.0.0",
      "inLanguage": "es",
      "offers": {
        "@type": "Offer",
        "price": "0.00",
        "priceCurrency": "USD"
      },
      "image": socialImage,
      "screenshot": socialImage,
      "publisher": {
        "@type": "Organization",
        "@id": "https://decidelo.app/#organization",
        "name": "Decídelo.app",
        "url": "https://decidelo.app/",
        "logo": {
          "@type": "ImageObject",
          "url": "https://decidelo.app/favicon.svg"
        }
      }
    },
    {
      "@type": "WebSite",
      "@id": "https://decidelo.app/#website",
      "name": "Decídelo.app",
      "url": "https://decidelo.app/",
      "publisher": {
        "@id": "https://decidelo.app/#organization"
      }
    },
    {
      "@type": "BreadcrumbList",
      "@id": "https://decidelo.app/#breadcrumb",
      "itemListElement": [
        {
          "@type": "ListItem",
          "position": 1,
          "name": "Inicio",
          "item": "https://decidelo.app/"
        }
      ]
    }
  ]
};
---

<Layout title={title} description={description} schemaLD={schemaLD} showHomeHeader={true}>
  <Fragment slot="head">
    <!-- Newsreader itálica: acento tipográfico del sistema editorial, solo en home -->
    <link href="https://fonts.googleapis.com/css2?family=Newsreader:ital,wght@1,400&display=swap" rel="stylesheet">
  </Fragment>

  <!-- Canvas transparente para el efecto de confeti -->
  <canvas id="confetti-canvas" aria-hidden="true"></canvas>

  <!-- El envoltorio existe solo para poder reordenar VISUALMENTE estas
       secciones en móvil (con `order` de flex, ver el <style> de abajo) sin
       tocar el orden real del DOM: el rastreo y el SEO siguen viendo hero,
       cómo-funciona, ruleta, herramientas y artículo en este mismo orden.
       Envolver aquí en vez de hacerlo en Layout.astro evita convertir
       `<body>` en un contenedor flex para todas las páginas del sitio por una
       necesidad que es solo de la home. -->
  <div class="home-sections">
    <!-- ====================================================================
         HERO SECTION
         ==================================================================== -->
    <HomeHero />

    <!-- ====================================================================
         CÓMO FUNCIONA
         ==================================================================== -->
    <HomeHowItWorks />

    <!-- Espacio Publicitario Superior -->
    <AdSlot position="top" />

    <!-- ====================================================================
         SECCIÓN DE LA RULETA — Herramienta Principal
         ==================================================================== -->
    <RouletteMachine />

    <!-- ====================================================================
         HUB DE HERRAMIENTAS
         ==================================================================== -->
    <HomeToolsSection />

    <!-- ====================================================================
         SECCIÓN DE ARTÍCULO SEO
         ==================================================================== -->
    <SeoArticle title="¿Qué es decídelo.app y para qué sirve?">
    <p><strong>Decídelo.app</strong> es un hub de herramientas de decisión aleatoria diseñado para resolver esos momentos de indecisión que todos enfrentamos a diario. Ya sea que necesites elegir qué cenar, sortear un ganador entre participantes, asignar tareas de forma imparcial o simplemente dejar que el destino tome la decisión por ti, nuestras herramientas hacen el trabajo pesado en cuestión de segundos.</p>
    <p>A diferencia de otros servicios similares, <strong>decídelo.app</strong> funciona 100% en tu navegador. Tus datos nunca salen de tu dispositivo: no hay servidores externos, no hay cookies de rastreo, y no hay necesidad de registro. Todo se almacena localmente mediante la tecnología <code>localStorage</code>, garantizando tu privacidad total.</p>

    <h2>La Ruleta Aleatoria Online: Tu aliada contra la indecisión</h2>
    <p>Nuestra herramienta estrella es la <strong>Ruleta Aleatoria Online</strong>. Construida con tecnología <strong>HTML5 Canvas</strong> de última generación, ofrece una experiencia visual fluida a 60 fotogramas por segundo con un algoritmo de desaceleración basado en física de fricción real. Cada giro se siente orgánico, impredecible y emocionante.</p>

    <h3>¿Cómo funciona la Ruleta?</h3>
    <ul>
      <li><strong>Escribe tus opciones</strong> en el panel lateral (una por línea). La ruleta se actualiza en tiempo real.</li>
      <li><strong>Personaliza el título</strong> haciendo clic sobre él. Ideal para sorteos con nombre: "¿Quién lava los platos?".</li>
      <li><strong>Presiona GIRAR</strong> y observa cómo la rueda cobra vida con efectos de sonido sintetizados y un puntero que se sacude al pasar por cada segmento.</li>
      <li><strong>¡Resultado con confeti!</strong> Al detenerse, un modal elegante revela al ganador con una celebración visual de confeti animado.</li>
      <li><strong>Oculta opciones</strong> sin borrarlas. Perfecto para torneos por eliminación o para ir descartando candidatos ronda a ronda.</li>
    </ul>

    <h2>Casos de uso populares</h2>

    <h3>Sorteos de nombres y ganadores</h3>
    <p>Ya sea para dinamizar tus sorteos en redes sociales (Instagram, TikTok, Facebook), seleccionar ganadores en concursos, o elegir el orden de exposición de estudiantes en clase, la <em>ruleta de nombres</em> garantiza imparcialidad absoluta. El público confía en un resultado visualmente aleatorio más que en un simple número generado por computadora.</p>

    <h3>Decidir qué comer</h3>
    <p>El clásico dilema de parejas y amigos. Introduce opciones como "Pizza 🍕", "Tacos 🌮", "Sushi 🍣" o "Hamburguesas 🍔" y deja que el destino elija el menú del día. Elimina discusiones innecesarias de forma divertida y democrática.</p>

    <h3>Dinámicas de clase y gamificación</h3>
    <p>Los profesores y facilitadores utilizan esta herramienta para hacer preguntas aleatorias, asignar tareas, seleccionar equipos o elegir temas de debate. La ruleta mantiene a los participantes alerta, atentos y comprometidos porque cualquiera puede ser el próximo.</p>

    <h3>Juegos y entretenimiento</h3>
    <p>Úsala como una ruleta de la fortuna, verdad o reto, penitencias para fiestas o para determinar premios en eventos. Personalízala con retos, castigos, recompensas o cualquier contenido creativo que se te ocurra.</p>

    <h2>Diseño accesible y enfoque en la concentración</h2>
    <p>Hemos diseñado <strong>decídelo.app</strong> con especial atención a la accesibilidad y experiencia de usuario. Nuestro <strong>Modo Foco</strong> elimina todas las distracciones de la pantalla (texto informativo, herramientas secundarias, anuncios) y te permite centrarte exclusivamente en la ruleta y tus opciones. Este modo es especialmente útil para personas con TDAH o que simplemente prefieren una interfaz limpia.</p>
    <p>El elegante modo oscuro predeterminado con una paleta cuidadosamente seleccionada reduce la fatiga ocular durante sesiones prolongadas. Los sonidos físicos sintéticos (totalmente opcionales) proporcionan retroalimentación sensorial sin ser intrusivos.</p>

    <h2>¿Por qué decídelo.app es diferente?</h2>
    <ul>
      <li><strong>Privacidad real:</strong> Sin servidores, sin cookies, sin registro. Todo funciona localmente en tu navegador.</li>
      <li><strong>Velocidad:</strong> Carga instantánea. No hay dependencias pesadas ni frameworks innecesarios.</li>
      <li><strong>Diseño premium:</strong> Cada detalle está cuidado: desde las micro-animaciones hasta la paleta de colores.</li>
      <li><strong>Accesibilidad:</strong> Compatible con lectores de pantalla, navegación por teclado y preferencias de movimiento reducido.</li>
      <li><strong>Offline-ready:</strong> Una vez cargada, funciona sin conexión a internet.</li>
    </ul>
  </SeoArticle>

    <!-- Espacio Publicitario Inferior -->
    <AdSlot position="bottom" />

    <!-- ====================================================================
         MODAL DE GANADOR
         ==================================================================== -->
    <WinnerModal />
  </div>

  <!-- Carga del JavaScript específico de la Ruleta -->
  <script src="../scripts/roulette.js"></script>
</Layout>

<style>
  /* La portada usa una cabecera de sección mayor que el resto del sitio.
     Base compartida en global.css; aquí solo lo que cambia.

     Van con :global() porque la cabecera de la sección de herramientas vive
     ahora en HomeToolsSection.astro, y el scope de esta página ya no la
     alcanza. Sin esto el titular volvía al tamaño pequeño y la home encogía
     50px en móvil. Solo la home carga este CSS, así que no se filtran. */
  :global(.section-header) { margin-bottom: 2.5rem; }
  /* Mismo kicker gris que ya usaba "Cómo funciona": el default de
     global.css es --accent-mint (acento neón de las páginas sin migrar),
     que aquí desentonaba con el acento cálido único del sistema editorial. */
  :global(.section-tag) { margin-bottom: 0.75rem; color: var(--text-secondary); }
  /* Mismo tamaño para el h2 de "Cómo funciona" y el del índice de
     herramientas: son secciones hermanas y deben leer con la misma
     jerarquía, un escalón por debajo del titular del hero. */
  :global(.section-heading) { font-size: clamp(1.65rem, 4vw, 2.3rem); }

  /* ====================================================================
     REORDEN VISUAL EN MÓVIL — decisión del propietario, no renegociable.
     ====================================================================
     Bajo 860px (el mismo breakpoint que ya separa el grid de dos columnas
     de la ruleta) la ruleta pasa a verse primero, antes que el hero y el
     "cómo funciona": es la herramienta, así que es lo primero que hay que
     poder usar sin scroll. El DOM no cambia -- solo el orden de pintado --
     así que el rastreo/SEO sigue viendo hero → cómo-funciona → ruleta →
     herramientas → artículo, que es el orden narrativo real de la página.

     Con flex, los elementos sin `order` explícito valen 0 y mantienen su
     orden de documento entre sí; solo hace falta sacar la ruleta a -1 para
     que se adelante a todos los demás. Como el anuncio superior
     (`<AdSlot position="top">`) vivía en el DOM justo antes de la ruleta,
     este único cambio también lo deja, de rebote, debajo de la rueda en
     vez de encima -- exactamente lo que pide la decisión 2 del encargo.
     Revertir la decisión 1 (quitar esta regla) revierte también ese efecto
     lateral, así que la decisión 2 no necesita una regla propia: no hay
     nada que hacer con el anuncio que no haga ya esta única línea. */
  @media (max-width: 859px) {
    .home-sections {
      display: flex;
      flex-direction: column;
    }

    .home-sections > :global(.roulette-section) {
      order: -1;
    }

    /* Decisión 2, aparte y explícita a propósito aunque hoy coincida con el
       efecto de la regla de arriba: el propietario sabe que el anuncio
       superior pierde visibilidad al quedar bajo la rueda, y quiere poder
       deshacer *esta* decisión sola sin tocar la del reordenamiento
       general. Para volver a poner el anuncio arriba de la rueda, basta con
       darle aquí un order menor que -1 (p. ej. -2); para "revertir del
       todo", con borrar esta regla alcanza -- sin ella, el anuncio ya
       queda debajo de la rueda igual, por el orden de documento normal. */
    .home-sections > :global([data-ad-paused="top"]) {
      order: 0;
    }
  }

  #confetti-canvas {
    position: fixed;
    top: 0; left: 0;
    width: 100vw; height: 100vh;
    pointer-events: none;
    z-index: 99;
  }

  /* Focus Mode Styles */
  /* .hero y .hub-section van con :global() porque su marcado vive ahora en
     HomeHero.astro y HomeToolsSection.astro: con el scope de esta página ya
     no los alcanzaría. El test de estados comprueba que siguen ocultándose
     (npm run test:estado). */
  body.focus-mode-active {
    background-color: #060608;
  }

  body.focus-mode-active :global(header) {
    background: transparent;
    border-bottom: none;
  }

  body.focus-mode-active :global(.logo-link),
  body.focus-mode-active :global(.hero),
  body.focus-mode-active :global(.how-it-works),
  body.focus-mode-active :global(.hub-section),
  body.focus-mode-active :global(.seo-section),
  body.focus-mode-active :global(.ad-placeholder),
  body.focus-mode-active :global(footer),
  body.focus-mode-active :global(.nav-link) {
    display: none !important;
  }

  body.focus-mode-active :global(.header-inner) {
    justify-content: flex-end;
  }

  body.focus-mode-active :global(.roulette-section) {
    min-height: calc(100vh - 80px);
    display: flex;
    align-items: center;
    padding: 0;
  }

  body.focus-mode-active :global(.roulette-section > .wrap) {
    width: 100%;
  }

  body.focus-mode-active :global(.roulette-section .section-header) {
    display: none;
  }

  body.focus-mode-active :global(.app-grid) {
    align-items: center;
  }

  @media (min-width: 860px) {
    body.focus-mode-active :global(.app-grid) {
      max-width: 950px;
      margin: 0 auto;
    }
  }

</style>
